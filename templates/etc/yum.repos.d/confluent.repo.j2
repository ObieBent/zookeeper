[Confluent.dist]
name=Confluent repository (dist)
baseurl=http://packages.confluent.io/rpm/{{ confluent_pkg_version }}/{% if confluent_pkg_version | int < 6 %}7{% else %}{{ ansible_distribution_major_version }}{% endif %}

gpgcheck=1
gpgkey=http://packages.confluent.io/rpm/{{ confluent_pkg_version }}/archive.key
enabled=1

[Confluent]
name=Confluent repository
baseurl=http://packages.confluent.io/rpm/{{ confluent_pkg_version }}
gpgcheck=1
gpgkey=http://packages.confluent.io/rpm/{{ confluent_pkg_version }}/archive.key
enabled=1
